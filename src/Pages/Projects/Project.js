import React from 'react';

const Project = (props) => {
    console.log(props.project)
    return (
        <div>
            <h1>this is single project</h1>
        </div>
    );
};

export default Project;